{
  "name": "ax-sales-navigator",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend && node scripts/prepare-sites.mjs",
    "preview": "npm run preview --workspace frontend"
  }
}
